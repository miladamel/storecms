
module.exports = {
    recaptcha : {
        client_key : process.env.RECAPTCHA_CLIENTKEY,
        site_key : process.env.RECAPTCHA_SITEKEY,
        options : {
            hl : 'fa'
        }
    }
}